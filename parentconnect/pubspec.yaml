name: parentconnect
description: "Application ParentConnect"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Navigation
  go_router: ^17.2.3

  # Requêtes HTTP vers Node.js
  http: ^1.2.1

  # Stockage local (JWT Token)
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Assets (à décommenter si tu ajoutes des images)
  # assets:
  #   - assets/images/

  # Fonts (optionnel)
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700